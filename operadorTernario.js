
//Operador ternário:

// ? == if // : == else
// Cliente com mais de 100 pontos = premium, se não = comum;

let pontos = 100;
let tipo = pontos >= 100 ? "Premium" : "Comum";
// Se 'pontos' >= 100, tipo = "Premium", se não, tipo = "Comum"

console.log(tipo);